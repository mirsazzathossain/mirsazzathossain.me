name: Sync Availability

on:
  schedule:
    - cron: "0 0 * * *"  # midnight UTC = 06:00 Dhaka (UTC+6)
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install google-api-python-client google-auth

      - name: Sync availability from Google Calendar
        run: python .github/scripts/sync_availability.py
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GOOGLE_CALENDAR_IDS: ${{ secrets.GOOGLE_CALENDAR_IDS }}

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet src/data/availability.json || (
            git add src/data/availability.json &&
            git commit -m "chore: sync weekly availability [skip ci]"
          )

      - name: Push
        run: git push
